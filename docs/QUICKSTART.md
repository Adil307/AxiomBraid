# AxiomBraid Quickstart

```python
import axiombraid as AB

result = AB.inspect("students.csv")
AB.report("students.csv")
cleaned = AB.clean("students.csv")
```

`AB` is the official documentation alias. Python aliases are user-defined,
but all AxiomBraid examples consistently use uppercase `AB` as requested.
