# Public API Reference — 0.9

Stable top-level functions: `read_csv`, `read_excel`, `guide`, `inspect`,
`report`, `clean`, `validate`, `compare`, `detect_drift`, `export_html`,
`stream_csv`, and `cached_inspect`.

Advanced class API: `Guide` (`DataGuide` remains an alias).
